[아우라딜마켓]

> 250725 시연 완료

- FO 퍼블리싱, API 연동
- BO 퍼블리싱, API 연동

## WIL

- .eslintrc -> 구버전, eslint.config -> 신버전
- input value 천단위 등 포메팅할 때 onChange 공통 함수에서 특정 필드에 대해 숫자만 추출하고(이로 인해 type=text를 써도 무방함), value에 값을 정의할 때 포메팅 함수로 감싸주는 것이 가장 간단하고 깔끔한 듯
- useMutate의 onSuccess 로직은 마운트가 끊겨버리면 동작하지 않음. 즉 onSuccess 로직을 사용할 때, 라우팅 로직을 먼저 사용하거나 혹은 병렬적으로 사용한다면 onSuccess는 동작하지 않게 됨. 그러므로 onSuccess에서 상태 저장 등의 로직을 쓴다면 해당 로직의 제일 뒷부분에서 라우팅 등을 진행할 것.
- 컴포넌트에서 ...rest 문법을 사용하면 aria-_, data-_ 같은 추가적인 HTML 속성을 유연하게 받아 내부 DOM 요소에 그대로 전달할 수 있다.
