# 1. Debounce와 Computed의 충돌 상황

검색어 입력 시 매번 API가 호출되어 성능 저하가 발생하여, Debounce를 적용했지만 작동하지 않는 문제

## 원인 분석

### Debounce가 적용되지 않은 이유

```typescript
// ❌ 문제가 있던 코드
const filterSearchword = ref('');

useGetAcccategorySelApi(
  computed(() => ({
    acc_name: filterSearchword.value, // 직접 참조
    ...
  }))
);

watch(filterSearchword, () => {
  debouncedSearch(); // debounce 함수 호출
});
```

**문제점:**

- `useQuery`의 `computed`에서 `filterSearchword.value`를 직접 참조
- `filterSearchword`가 변경되면 즉시 `computed`가 재계산되어 API 호출
- Debounce 함수가 실행되기 전에 이미 API가 호출됨

## 해결 방법

### 1. Debounced 값 분리

```typescript
const filterSearchword = ref(''); // 사용자 입력값
const debouncedSearchWord = ref(''); // debounce된 검색어
```

### 2. useQuery에서 debounced 값 사용

```typescript
// ✅ 개선된 코드
useGetAcccategorySelApi(
	computed(() => ({
		acc_name: debouncedSearchWord.value, // debounced 값 사용
		ex_accessories_pk: props.excludedPks || '',
		stock_warehouse_pk: props.stockWarehousePk,
	})),
	computed(() => !props.useThirdCategory)
);
```

### 3. Debounce 함수 구현

```typescript
import { debounce } from 'lodash-es';

// debounce된 검색어 업데이트 함수
const updateDebouncedSearchWord = debounce((value: string) => {
	debouncedSearchWord.value = value;
}, 500);

// filterSearchword 변경 시 debounce된 검색어 업데이트
watch(filterSearchword, (newValue) => {
	updateDebouncedSearchWord(newValue);
});
```

### 4. 즉시 검색 기능 유지

```typescript
// 검색 버튼 클릭 또는 엔터 키 입력 시 즉시 검색
const onSearchFilter = () => {
	page.value = 1;
	if (props.useThirdCategory) {
		accThirdCategoryRefetch();
	} else {
		acccategoryRefetch();
	}
};
```

## 핵심 학습 포인트

### Debounce와 Reactive 값의 분리

1. **Reactive 값의 즉시 반응 특성**

   - Vue의 `ref`나 `computed`는 값이 변경되면 즉시 반응
   - `useQuery`의 `computed` 파라미터에 직접 사용하면 즉시 API 호출

2. **Debounce 적용 방법**

   - 사용자 입력값(`filterSearchword`)과 API 파라미터 값(`debouncedSearchWord`)을 분리
   - Debounce는 값 업데이트에만 적용
   - `useQuery`는 debounced 값을 참조하여 자동 호출

3. **동작 흐름**
   ```
   사용자 입력 → filterSearchword 변경
                ↓
   watch 트리거 → updateDebouncedSearchWord (debounce 500ms)
                ↓
   500ms 후 → debouncedSearchWord 업데이트
                ↓
   computed 재계산 → useQuery 자동 호출
   ```

## 개선 효과

1. **성능 향상**

   - 타이핑 중 불필요한 API 호출 제거
   - 500ms 대기 후 한 번만 API 호출

2. **사용자 경험**
   - 자연스러운 검색 동작
   - 검색 버튼 클릭 시 즉시 검색 (debounce 우회)

## 참고 사항

- Debounce 시간: 500ms (사용자 입력 후 0.5초 대기)
- 검색 버튼 클릭 또는 엔터 키 입력 시 즉시 검색
- 모달 초기화 시 `debouncedSearchWord`도 함께 초기화 필요
