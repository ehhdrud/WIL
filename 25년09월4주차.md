# [대가의선택]

- 일본주식 수정사항 반영

# [대한화장품학회]

- 수정사항 반영

# [VAN365]

- 유지보수 처리

---

# 📌 WIL

## 테마 기반 자동 반응형 스타일링 시스템

### 개요

테마에 디바이스 상태를 포함시켜서 styled-components에서 `theme.device.isMobile`로 직접 접근하는 CSS-in-JS 기반 반응형 시스템입니다.

### 핵심 장점

- **Props 전달 불필요**: 컴포넌트 코드가 깔끔해짐
- **자동 동기화**: 디바이스 상태 변경 시 모든 스타일 자동 업데이트
- **타입 안전성**: TypeScript로 자동완성 및 타입 체크
- **중앙 집중식**: 테마 하나로 모든 반응형 로직 관리

### 구현 단계

#### 1단계: 테마 확장

```typescript
// src/styles/theme.ts
const theme: Theme = {
	colors,
	fontSize,
	fontWeight,
	device: {
		// ← 디바이스 상태 추가
		isMobile: false,
		breakpoint: 768,
	},
};
```

#### 2단계: TypeScript 타입 정의

```typescript
// src/styles/emotion.d.ts
declare module '@emotion/react' {
	export interface Theme {
		colors: ColorsTypes;
		fontSize: FontSizeTypes;
		fontWeight: FontWeightTypes;
		device: {
			// ← 테마 타입 확장
			isMobile: boolean;
			breakpoint: number;
		};
	}
}
```

#### 3단계: 테마 동기화 훅 생성

```typescript
// src/hooks/useDeviceThemeSync.ts
export const useDeviceThemeSync = () => {
	const theme = useTheme();
	const { isMobile } = useDeviceStore();

	useEffect(() => {
		// Zustand 상태 → 테마 상태로 동기화
		if (theme.device) {
			theme.device.isMobile = isMobile;
		}
	}, [isMobile, theme]);
};
```

#### 4단계: App.tsx에서 동기화 활성화

```typescript
// src/App.tsx
function App() {
	const initializeDeviceDetection = useDeviceStore((state) => state.initializeDeviceDetection);

	useDeviceThemeSync(); // ← 테마 동기화 활성화

	useEffect(() => {
		const cleanup = initializeDeviceDetection();
		return cleanup;
	}, [initializeDeviceDetection]);

	return <ThemeProvider theme={theme}>{/* ... */}</ThemeProvider>;
}
```

#### 5단계: styled-components에서 테마 사용

```typescript
// src/components/common/Footer/Footer.style.ts
export const PolicyButton = styled.button`
	font-size: ${({ theme }) => (theme.device.isMobile ? '1.2rem' : '1.4rem')};
	// ↑ props 없이 테마에서 직접 접근
`;

export const ContactSection = styled.div`
	gap: ${({ theme }) => (theme.device.isMobile ? '1rem' : '1.2rem')};
	// ↑ 모든 스타일에서 theme.device.isMobile 사용 가능
`;
```

#### 6단계: 컴포넌트 간소화

```typescript
// src/components/common/Footer/Footer.tsx
const Footer: React.FC = () => {
	return (
		<S.FooterWrapper>
			<S.FooterContent>
				{' '}
				{/* ← props 없음! */}
				<S.PolicyButton>개인정보처리방침</S.PolicyButton>
				{/* ↑ 모든 컴포넌트가 props 없이 동작 */}
			</S.FooterContent>
		</S.FooterWrapper>
	);
};
```

### 동작 흐름

1. **화면 크기 변경** → Zustand 스토어 업데이트
2. **useDeviceThemeSync** → 테마 상태 동기화
3. **styled-components** → 테마 상태 기반으로 스타일 재계산
4. **자동 리렌더링** → 모든 컴포넌트가 새로운 스타일 적용

### 사용법 비교

#### 기존 방식 (번거로움)

```typescript
// 컴포넌트에서 props 전달 필요
const { isMobile } = useDeviceStore();
<Component isMobile={isMobile} />;

// 스타일에서 props 받아서 사용
const StyledComponent = styled.div<{ isMobile: boolean }>`
	font-size: ${({ isMobile }) => (isMobile ? '1.2rem' : '1.4rem')};
`;
```

#### 새로운 방식 (간단함)

```typescript
// 컴포넌트에서는 아무것도 하지 않음!
<Component />;

// 스타일에서만 테마 사용
const StyledComponent = styled.div`
	font-size: ${({ theme }) => (theme.device.isMobile ? '1.2rem' : '1.4rem')};
	gap: ${({ theme }) => (theme.device.isMobile ? '1rem' : '1.2rem')};
`;
```

### 파일 구조

```
src/
├── styles/
│   ├── theme.ts              # 테마 확장
│   └── emotion.d.ts          # 타입 정의
├── hooks/
│   └── useDeviceThemeSync.ts # 테마 동기화 훅
├── stores/
│   └── deviceStore.ts        # 디바이스 상태 관리
└── components/
    └── common/
        └── Footer/
            ├── Footer.tsx    # 간소화된 컴포넌트
            └── Footer.style.ts # 테마 기반 스타일
```

### 브레이크포인트 설정

- **모바일**: 768px 미만
- **데스크톱**: 768px 이상
- **브레이크포인트 변경**: `DEVICE_BREAKPOINT.mobile` 값 수정

### 주의사항

1. **테마 동기화**: App.tsx에서 `useDeviceThemeSync()` 호출 필수
2. **타입 안전성**: emotion.d.ts 파일에서 타입 정의 필요
3. **성능**: 테마 상태 변경 시 모든 styled-components가 리렌더링됨

### 확장 가능성

- 추가 디바이스 타입 (태블릿, 데스크톱 등)
- 다중 브레이크포인트 지원
- 테마별 디바이스 설정
- 애니메이션 상태 관리

---

- `overflow: scroll`은 스크롤이 안 생길때 스크롤이 생기는 영역이 강제적으로 잡힌다. 개발자 도구 Element에서 영역 우측과 하단에 padding이 없음에도 padding 잡혀있는 것처럼 노출됨. `auto` 속성 사용 권장

- Axios로 서버의 API를 호출하고, `responseType: 'blob'`으로 PDF를 받아 Blob 객체로 처리한 뒤, `URL.createObjectURL()`과 `<a download>`를 이용해 파일을 바로 다운로드한다. 프론트는 PDF 생성에는 관여하지 않고, 문서 유형에 따라 파일명을 지정한다.
