# 문제 요약

## 🎯 핵심 문제

react-hook-form의 register가 InputField 컴포넌트에서 값을 제대로 감지하지 못하는 문제

## 🔍 문제 발생 과정

- **초기 상황**: Signup.tsx에서 `{...register('email')}`을 사용했지만 값이 감지되지 않음
- **디버깅 과정**:
  - `e.target.value`는 찍히지만 `getValues()`에는 undefined가 들어감
  - required 에러가 불필요하게 발생
  - 다양한 설정 변경 시도 (mode, defaultValues 등)
- **근본 원인 발견**:
  - "Function components cannot be given refs" 에러 발생
  - react-hook-form의 register는 내부적으로 ref를 사용하는데, InputField가 forwardRef로 래핑되지 않았음

## ✅ 해결 방법

InputField 컴포넌트를 `React.forwardRef`로 수정

## 📚 학습 포인트

- **react-hook-form의 동작 원리**: register는 ref를 통해 DOM 요소에 직접 접근
- **forwardRef의 중요성**: 커스텀 컴포넌트에서 ref를 전달하려면 반드시 forwardRef 필요
- **에러 메시지의 의미**: "Function components cannot be given refs"는 forwardRef 누락을 알려주는 신호

## 🎉 결과

이제 Signup.tsx에서 `{...register('email')}`이 정상적으로 작동하여 폼 값이 제대로 감지됩니다!

핵심은 react-hook-form과 커스텀 컴포넌트를 함께 사용할 때는 반드시 forwardRef를 사용해야 한다는 점이었습니다.
